app:
  name: "TLM_Cui"
  org: "com.cui.tlmapp"
  version: "1.3.0"       
  build_number: 2        

# Android 图标配置（核心）
android:
  icon: "icon.png"  # 确保文件在根目录，名字完全匹配（大小写敏感！）

# iOS 图标配置（可选，不打包iOS可删）
ios:
  icon: "Icon-App.png"

# Windows 图标配置（可选，不打包Windows可删）
windows:
  icon: "icon.ico"

# macOS 图标配置（可选，不打包macOS可删）
macos:
  icon: "icon.png"
